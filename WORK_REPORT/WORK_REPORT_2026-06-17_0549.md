# WORK_REPORT 2026-06-17 05:49

## 完了した作業

- `personal_website`, `group_website`, `site_data` のディレクトリ構成を確認した。
- `group_website` と `personal_website` のページ構成、metadata、Markdown/YAML データの読み込み方を確認した。
- `group_website` の未完成点を洗い出した。
  - `group_website/app/layout.tsx` が `@/personal/_metadata` を参照している。
  - `group_website/app/ui/client/langBotton.tsx` が `@/personal/_metadata` を参照している。
  - top page に「適当に変える」という仮文が残っている。
  - `site_data/group/markdown_pages/ja/contact.md` に `test` / `hello` が残っている。
  - `site_data/group/markdown_pages/*/project.md` に frontmatter がない。
  - `site_data/group/members.yml` に重複ダミーデータと外部 demo 画像が残っている。
  - group 用 sitemap/robots/CI が未整備。
- ユーザー方針を確認した。
  - 当面は `https://kazuki-yamamoto.github.io/group/` のような GitHub Pages subpath 公開を想定する。
  - 将来の大学ドメイン移行に備えて、personal と group は分離した構造を維持する。
  - 最優先は公開可能な土台。
  - members は schema だけ先に固める。
- 複数セッションで使う計画書 `GROUP_WEBSITE_PLAN.md` を作成した。

## 実行した確認

- `group_website`
  - `npm run test -- --run`: 成功。
  - `npm run build`: 成功。
  - `npm run lint`: 失敗。ESLint flat config が存在しないため。
- `personal_website`
  - `npm run test -- --run`: 成功。
  - `npm run build`: 成功。
  - `npm run lint`: 失敗。ESLint flat config が存在しないため。
- `git status --short`
  - 作業開始時点から `AGENT.md` が untracked。
  - このセッションで `GROUP_WEBSITE_PLAN.md` と本レポートを追加。

## Slurm ジョブ

- なし。

## 残タスク

- `group_website` を `/group` 配下公開に対応させる。
- group 内の `@/personal/_metadata` 参照を撤去する。
- raw asset path、Markdown 内部リンク、metadata URL を basePath 対応にする。
- lint 設定を復旧する。
- members schema を固定し、重複ダミーと外部 demo 画像を撤去する。
- contact/project/top page の仮文を本番向けに整理する。
- personal と group を 1 つの GitHub Pages artifact に合成する CI を整備する。

## 次のアクション

1. `GROUP_WEBSITE_PLAN.md` の Session 1 から開始する。
2. `site_data/group/_metadata.js` に `basePath` と GitHub Pages 用 `publicURL` 方針を反映する。
3. group 用 path helper を作り、raw assets と metadata を `/group` 対応にする。
4. `rg '@/personal' group_website/app` が 0 件になるまで metadata import を修正する。
5. `cd group_website && npm run test -- --run && npm run build` で確認する。
