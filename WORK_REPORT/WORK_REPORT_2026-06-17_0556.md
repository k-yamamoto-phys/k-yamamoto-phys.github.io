# WORK_REPORT 2026-06-17 05:56

## 完了した作業

- `group_website` を `/group` 配下公開に対応させる Session 1 を実装した。
- `site_data/group/_metadata.js` に `basePath` と `publicURL` を追加した。
  - default: `basePath = "/group"`
  - default: `publicURL = "https://kazuki-yamamoto.github.io/group"`
  - `NEXT_PUBLIC_GROUP_BASE_PATH` と `NEXT_PUBLIC_GROUP_PUBLIC_URL` で上書き可能。
- `group_website/next.config.ts` に `basePath` 設定を追加した。
- `group_website/app/lib/site-paths.ts` を追加した。
  - `withBasePath`
  - `withoutBasePath`
  - `siteUrl`
- `group_website/app/layout.tsx` と `group_website/app/ui/client/langBotton.tsx` の `@/personal/_metadata` 参照を撤去した。
- Markdown 変換後の `img[src]` と内部 `a[href]` を `/group` 対応にした。
- Hero 背景画像、トップページ画像、members の集合写真・メンバー写真を basePath helper 経由にした。
- metadata の canonical、Open Graph、Twitter image、alternate language URL を `https://kazuki-yamamoto.github.io/group/...` 形式に統一した。
- `[other_pages]` と `LangSetting` の build 時 debug log を削除した。
- `activities` 英語ページの戻り先を `/en` から `/` に修正した。

## 実行した確認

- `cd group_website && npm run test -- --run`
  - 成功。`app/lib/markdown.test.ts` の 7 tests passed。
- `cd group_website && npm run build`
  - 成功。
  - daisyUI の CSS `@property` warning は従来通り出るが、build は成功。
- `rg '@/personal' group_website/app`
  - 0 件。
- `rg 'console\.log' group_website/app`
  - 0 件。
- `rg 'src="/images|href="/(members|activities|research|publications|presentations|contact|project)|url\(/images' group_website/out`
  - 0 件。
- `group_website/out/index.html` と `group_website/out/_next` の存在を確認した。
- 出力 HTML で以下を確認した。
  - internal links は `/group/...`。
  - `_next` assets は `/group/_next/...`。
  - Markdown 画像は `/group/images/...`。
  - canonical/OGP/Twitter image は `https://kazuki-yamamoto.github.io/group/...`。
  - `/group/group` の二重 prefix は 0 件。

## Slurm ジョブ

- なし。

## 残タスク

- lint 復旧は未実施。`eslint.config.*` の追加は Session 2 で扱う。
- combined GitHub Pages workflow は未実施。Session 5 で personal/group artifact 合成を行う。
- members schema 固定、重複ダミー撤去、外部 demo 画像撤去は未実施。Session 3 で扱う。
- top/contact/project の仮文・frontmatter 整理は未実施。Session 3 で扱う。
- README/DESIGN 更新は未実施。後続セッションで実装状態に合わせて更新する。

## 次のアクション

1. Session 2 として ESLint flat config を整備し、`npm run lint` を両サイトで通す。
2. その後、Session 3 で content contract と members schema を固める。
3. CI 合成前に、`group_website/out` を `staging/group/` へ copy して `serve staging` で `/group/` 配下動作を確認する。
